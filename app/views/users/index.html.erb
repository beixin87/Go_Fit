<% provide(:title, 'All users') %>
<h1>All users</h1>
<p> User Count : <%= @userCnt %></p>
<ul class="users">
  <% @users.select {|u| !u.admin }.each do |user| %>
    <%= render user %>

  <% end %>

  <br>
  <br>
  <%= will_paginate @users %>
</ul>
