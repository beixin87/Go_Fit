<% provide(:title, 'All users') %>
<h1>All users</h1>
 <p> User Count : <%= @users.userCnt %></p>
<ul class="users">
  <% @users.select {|u| !u.admin }.each do |user| %>
    <%= render user %>

  <% end %>
  <%= will_paginate @users %>
</ul>
