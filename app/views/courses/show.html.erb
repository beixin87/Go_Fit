<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @course.name %>
</p>
<p>
  <strong>Instrutor:</strong>
  <%= @instrutor.name %>
</p>
<p>
  <strong>Limit:</strong>
  <%= @course.limit %>
</p>

<p>
  <strong>Fee:</strong>
  <%= @course.fee %>
</p>

<p>
  <strong>Numberofenrolled:</strong>
  <%= @course.numberofenrolled %>
</p>

<p>
  <strong>Start:</strong>
  <%= @course.start %>
</p>

<p>
  <strong>End:</strong>
  <%= @course.end %>
</p>
<table class="table table-striped">
  <th>
    <tr>
      <th>Students</th>
    </tr>
  </th>
  <tb>
    <% @user_courses.each do |user_course| %>
    <tr>
      <td><%= user_course.name %></td>
      <% if current_user.admin? && !current_user?(user) %>
      <td>
        <%= link_to 'Delete', user_course_path(user_course),
                                :method => :delete,
                                :data => { :confirm => "Are you sure?" },
                                :class => "btn btn-xs btn-danger" %>
      </td>
      <% end %>
    </tr>
    <% end %>
  </tb>

<td><%= link_to 'Add to my Courses', user_courses_path(user: current_user, course_id: @course.id),
         class: 'btn btn-xs btn-success', method: :post  %></td>
<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
